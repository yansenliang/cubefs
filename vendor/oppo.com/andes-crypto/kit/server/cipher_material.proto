syntax = "proto3";

package server;

option go_package ="./";

// 加密材料。
message CipherMaterial {

    // 加密密钥
    bytes Key = 1;

    // 加密初始化向量
    bytes IV  = 2;

    // 是否计算HMAC
    bool Hmac = 3;
}
